<!DOCTYPE html>
<html>
<head>
  <title>My Blog Home</title>
</head>
<body>
  <h1>Welcome to My Blog</h1>
  <h2>Latest Posts</h2>
  <div class="swiper-wrapper">
    {% for post in collections.blog | reverse | slice(0,4) %}
      <div class="swiper-slide">
        <div class="bi-blog-item-3 headline pera-content position-relative" data-background="{{ post.data.image }}">
          <div class="bi-blog-author d-flex align-items-center">
            <div class="inner-img">
              <img src="{{ post.data.author_image or '/assets/img/blog/default-author.jpg' }}" alt="{{ post.data.author }}">
            </div>
            <div class="inner-text">
              <span>Written by</span>
              <h3>{{ post.data.author }}</h3>
            </div>
          </div>
          <div class="bi-blog-text">
            <div class="blog-meta text-uppercase">
              <i class="fas fa-calendar-alt"></i> {{ post.data.date | date("MMMM D, YYYY") }}
            </div>
            <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
            <p>
              {{ post.templateContent | striptags | truncate(160, true, '...') }}
            </p>
          </div>
          <a class="more_btn d-flex justify-content-between align-items-center position-absolute text-uppercase" href="{{ post.url }}">Learn More <i class="fal fa-long-arrow-right"></i></a>
        </div>
      </div>
    {% endfor %}
  </div>
  <a href="/blog/">See all blog posts</a>
</body>
</html>